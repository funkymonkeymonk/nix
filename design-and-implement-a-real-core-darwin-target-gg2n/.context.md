# Outcome
Devenv now sources canonical package definitions from modules/roles/foundation-packages.nix and modules/roles/developer-packages.nix. The developer role consumes the same reusable developer package list. The obsolete library/dev-base.nix and modules/darwin/dev-base.nix parallel definitions were removed. Devenv excludes gomuks from its projection because the generated devenv package set does not honor the repository insecure-package policy; the NixOS developer role retains it.

# Verification
- nix build .#checks.x86_64-linux.dev-base-migration --no-link passes.
- nix flake check --no-build --system x86_64-linux passes.
- all-role tests pass.
- alejandra --check on changed files passes.
- devenv tasks run check:lint remains blocked by the generated .devenv.flake.nix pointing at /home/monkey/repos/nix rather than this checkout.
