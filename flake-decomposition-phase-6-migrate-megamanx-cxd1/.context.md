# Problem
MegamanX is a Darwin daily driver (targets/MegamanX/default.nix, 149 lines). It's similar to wweaver but for the personal user "monkey". Uses roles: developer, desktop, workstation, entertainment, llm-host, opencode, pi, homebrew. Has Ollama, Vane, and complex opencode provider/agent config.

## Prerequisites
- Flake decomposition: Phase 5 — migrate core and bootstrap must be done

# Acceptance Criteria
- [ ] MegamanX-v2 added as parallel darwinConfiguration using library.lib.mkDarwinSystem
- [ ] Uses library/archetypes/developer-laptop-darwin.nix
- [ ] All machine-specific config from targets/MegamanX/default.nix preserved:
  - myConfig from mkUser "monkey" + all 8 role toggles
  - ollama host = "0.0.0.0"
  - vane config (embeddedSearxng, defaultModel=deepseek-r1:14b, embeddingModel=nomic-embed-text)
  - opencode complex config (model=ollama/qwen3.5, providers ollama + opencode-go, 3 custom agents, disabledProviders)
  - pi settings (theme, editor, agentsMd, models, prompts, skills)
  - system.primaryUser = "monkey"
- [ ] Import chain matches: configuration, nix-homebrew, modules/, modules/roles/homebrew.nix, modules/services/ollama/darwin.nix, modules/services/vane/darwin.nix, os/darwin.nix, modules/home-manager/aerospace.nix, home-manager.darwinModules.home-manager
- [ ] Old darwinConfigurations.MegamanX unchanged
- [ ] `nix store diff-closures .#MegamanX .#MegamanX-v2` is empty or only expected changes
- [ ] `devenv tasks run test:darwin-eval` passes

# Files
- flake.nix: current MegamanX at lines 287-302
- targets/MegamanX/default.nix: source of truth (149 lines)
- library/archetypes/developer-laptop-darwin.nix: enables homebrew, desktop, entertainment roles + SSH + openssh
- Note: MegamanX has roles.homebrew.enable=true — verify developer-laptop-darwin archetype already covers this (it does: homebrew.enable = true)

# Notes
- Second-to-last migration per design doc: "Protect daily drivers until last"
- wweaver comes after this, then Phase 8 cleanup
- mkDarwinSystem does NOT include home-manager or nix-homebrew modules — must be in modules list
- The opencode `model = lib.mkForce "ollama/qwen3.5"` uses mkForce — ensure override priority is maintained
