# Goal
Set up the new flake-parts monorepo structure without touching any existing machine configs.
All existing nixosConfigurations and darwinConfigurations must remain working after this phase.

# Reference
Full design: docs/plans/2026-05-11-flake-decomposition-design.md

# Prerequisites (must complete before Phase 0 structural work)
- Merge PR #291: NixOS auto-upgrade
- Merge PR #293: rename cattle account to admin
- Merge PR #284: deploy-rs protoman (merge as-is, deploy-rs is temporary)
- Move work LLM endpoint URLs to opnix
- Replace TAILSCALE_AUTH_KEY env var with opnix

# Archetype Naming Convention
Platform suffix only when platform-specific and cross-platform equivalent could exist:
- developer-laptop-darwin
- headless-server-darwin
- headless-server-nixos  (type-server + type-server-arm collapse into one)
- desktop-nixos
- microvm-guest
- nas  (platform-agnostic, deferred — added after Phase 0 via PR #283 rebase)

# Acceptance Criteria
- [ ] Add flake-parts input to flake.nix
- [ ] Create schema/flake.nix + schema/flake-module.nix with mkProfile and profile type
- [ ] Create schema/modules/profile.nix with myProfile options (user, roles, opencode, providers, skills, llmEndpoints)
- [ ] All schema options have defaults except user.name, user.email, user.fullName
- [ ] Create library/flake-module.nix wrapping existing modules/
- [ ] Create library/archetypes/ with all archetypes mapped from machine-types/ (see naming table above)
- [ ] type-server and type-server-arm collapse into headless-server-nixos (system param handles arch)
- [ ] Create library/lib/mk-system.nix with mkDarwinSystem and mkNixosSystem
- [ ] mkNixosSystem/mkDarwinSystem both accept system as optional param
- [ ] Overrides applied at priority 50 (beats profile at 100, beats defaults at 1000)
- [ ] Create profiles/wweaver/flake.nix + profile.nix with real wweaver preferences
- [ ] Create profiles/monkey/flake.nix + profile.nix with personal preferences
- [ ] devenv tasks run check:all passes with no regressions
- [ ] All existing nixosConfigurations and darwinConfigurations still build

# Files
- flake.nix (add flake-parts input)
- schema/ (new directory)
- library/flake-module.nix (new)
- library/archetypes/ (new, maps from machine-types/ with new names)
- library/lib/mk-system.nix (new)
- profiles/wweaver/ (new — work profile)
- profiles/monkey/ (new — personal profile)
