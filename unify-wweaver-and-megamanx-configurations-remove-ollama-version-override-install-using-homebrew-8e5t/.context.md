# Goal
Originally: unify wweaver and megamanx configurations, remove ollama overrides, use homebrew for ollama, and keep host files thin.

## Resolution
Superseded by the broader Phase 8 cleanup in PR #402. The refactor:
- Removed the developer-laptop-darwin archetype entirely and merged its roles into workstation-darwin.
- Both wweaver and MegamanX now use workstation-darwin + machine-specific overrides.
- Confirmed no ollama Nix overrides remain; homebrew role handles ollama on Darwin.
- Host files are thin overrides (wweaver 103 lines; MegamanX 218 lines, justified by its local LLM stack).

See yak: Standardize remaining hosts on archetypes for flake-parts migration.
