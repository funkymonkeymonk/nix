## Needs Refinement

Flagged by autonomous agent on 2026-08-22.

**Reason:** No context defined. Need Goal, Acceptance Criteria, and specifics on job configuration/triggers.

To unblock: add Goal, Acceptance Criteria with checkboxes, and specific file paths.

# Goal
Wire the 5 lightweight mkTest-based VM checks into pr-validation.yml so they actually run on PRs. Today they run nowhere: pr-validation.yml:490 explicitly filters out every check whose name starts with 'vm-' (select(startswith("vm-") | not)), and the local test:build-checks task runs 'nix flake check' for the CURRENT system only -- since these checks are gated to x86_64-linux and this repo is developed primarily on aarch64-darwin, they've likely never been built by anyone since they were written.

# Acceptance Criteria
- [ ] New job in .github/workflows/pr-validation.yml (modeled on the existing test-and-validate job's Nix/Cachix setup) builds: vm-users, vm-ssh, vm-packages, vm-role-foundation, vm-role-developer via `nix build ".#checks.x86_64-linux.$name" --no-link --print-build-logs`
- [ ] Job runs on a KVM-capable runner (nixosTest requires /dev/kvm) -- verify GitHub-hosted ubuntu-latest has KVM access enabled for this org before finalizing; if not, this may need self-hosted or a KVM-enabling step
- [ ] test-and-validate job's existing vm- exclusion filter (line 490) can stay as-is -- these 5 checks are now covered by the new dedicated job instead, no double-run
- [ ] Job triggers on the same PR events as the rest of pr-validation.yml (opened/synchronize/reopened), not gated to push-to-main (these are cheap, unlike the descoped e2e tier)
- [ ] All 5 checks pass in CI on a test PR before merging this change

# Files
.github/workflows/pr-validation.yml
