# Outcome
Added a dedicated KVM-backed VM checks job to pr-validation.yml. It builds vm-users, vm-ssh, vm-packages, vm-role-foundation, and vm-role-developer on pull requests, separate from the non-VM check loop.

# Verification
- yamllint .github/workflows/pr-validation.yml passes.
- nix flake check --no-build --system x86_64-linux passes.
- Local VM checks pass via test:build-checks.
- devenv task lint remains affected by the stale generated workspace path documented in the preceding PR.
