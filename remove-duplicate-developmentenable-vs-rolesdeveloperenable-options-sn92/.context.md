# Problem
`myConfig.development.enable` is a dead option:
- It is set to `true` in `modules/roles/developer.nix` when the developer role is enabled
- Nothing else reads it — no module consumes `myConfig.development.enable`
- The actual developer role check is `config.myConfig.roles.developer.enable`

This is pure dead code. The option is declared in `options.nix` but never consumed.

# Acceptance Criteria
- [ ] `myConfig.development` option block removed from `modules/common/options.nix`
- [ ] `myConfig.development.enable = true;` setter removed from `modules/roles/developer.nix`
- [ ] `devenv tasks run test:darwin-eval && devenv tasks run test:nixos-eval` pass
- [ ] No other module references `myConfig.development` (confirmed by grep)

# Files
- modules/common/options.nix (remove `development` option block, lines ~166-172)
- modules/roles/developer.nix (remove `myConfig.development.enable = true;` line 26)
