# Goal
Move 1Password Connect from protoman (Darwin) to a NixOS server for better reliability.

# Rationale
Running Connect on Darwin headless server has issues:
- Docker Desktop requires GUI session
- Podman needs VM setup (lima) which has compatibility issues
- Colima cannot run as root
- Manual start required after reboot

NixOS provides native systemd service support for containers.

# Architecture After Move
```
protoman (Darwin)          NixOS Server
├─ MicroVMs ─────────────► ├─ 1Password Connect
│  (fetch secrets)         │  (systemd + podman/docker)
└─ opnix (client) ───────► └─ Serves secrets to all MicroVMs
```

# Acceptance Criteria
- [ ] Deploy Connect on NixOS server (type-server or new host)
- [ ] Update darwin-server to use remote Connect (op://nixos-server:8080)
- [ ] Update MicroVMs to use remote Connect
- [ ] Migrate credentials via opnix
- [ ] Remove manual connect-start from darwin workflow
- [ ] Document new architecture

# Files to Modify
- targets/nixos-server/ or modules/nixos/connect.nix (new)
- targets/darwin-server/default.nix (update Connect URL)
- targets/microvms/openclaw.nix (update Connect URL)

# Notes
Can use existing NixOS host (type-server) or dedicate a MicroVM on protoman
running NixOS just for Connect if hardware allows.

## Needs Refinement

Flagged by autonomous agent on 2026-05-12.

**Reason:** The target NixOS server is not specified ('type-server or new host'). Moving 1Password Connect is a destructive operation involving live credentials. Without a clear target host and migration plan, autonomous implementation risks leaving MicroVMs without secret access. Please specify: (1) which NixOS host will run Connect, and (2) the migration sequence to avoid downtime.

To unblock: specify target host, migration sequence (old → new URL cutover), and whether MicroVMs can tolerate brief Connect downtime during migration.

## Decision (2026-08-22)

Target host: type-server directly -- NOT a dedicated MicroVM. Reuses existing infra, matches the stated reliability goal (native systemd, no manual restart-after-reboot) without added operational surface (network routing, another VM to patch/monitor). A MicroVM would only be justified for stronger credential-serving isolation, which is disproportionate for this repo's scale. Confirmed by Will 2026-08-23.

## Remaining open item
Migration sequence (old protoman URL -> new type-server URL cutover) and whether MicroVMs can tolerate brief Connect downtime during the swap still need to be specified before implementation -- this yak's original @needs-human reason. Target host is now decided; sequencing still needs Will's input when this is picked up.
