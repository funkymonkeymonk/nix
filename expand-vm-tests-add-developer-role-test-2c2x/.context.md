## Needs Refinement

Flagged by autonomous agent on 2026-08-22.

**Reason:** No context defined. Need Goal, Acceptance Criteria, and key files (existing vm test to extend).

To unblock: add Goal, Acceptance Criteria with checkboxes, and specific file paths.

## Resolution (2026-08-22)

Closed and merged into 'Add generic per-role VM package test generator (mkRoleVmTest)' (formerly 'Expand vm tests: add foundation role test'). Rather than hand-writing separate testScripts per role, one generator introspects each role's actual environment.systemPackages and produces both vm-role-foundation and vm-role-developer automatically. See that yak for full design.
