# Problem
wweaver is the primary Darwin machine (targets/wweaver/default.nix, 168 lines) and the ABSOLUTE LAST migration per the design doc. Most complex config: work laptop with Justworks provider, Ollama local, Vane with Colima, multiple opencode providers/agents/commands, pi agent.

The profile data in targets/wweaver/ already overlaps with profiles/wweaver/profile.nix (user, roles, opencode model, providers). The migration reconciles these — using the profile as the data source for user identity and preferences, with machine overrides for hardware-specific config.

Boundary decisions:
- opencode.extraMcpServers + opencode.commands → profile (user preference, follows the user across machines)
- opencode.providers with onePasswordItem refs → profile (already there at profiles/wweaver/profile.nix:29 — the item ref is a path, not a secret)
- Ollama host binding, model list → machine override (hardware-specific)
- Vane colima sizing → machine override (hardware-specific: cpu/memory/disk)
- homebrew.casks → machine override (platform-specific)

# Acceptance Criteria
- [ ] wweaver-v2 added as parallel darwinConfiguration using library.lib.mkDarwinSystem
- [ ] Uses library/archetypes/developer-laptop-darwin.nix
- [ ] Profile data from profiles/wweaver/profile.nix used as the user/role/provider source:
  - user: wweaver / wweaver@justworks.com / Will Weaver
  - roles: developer, desktop, workstation, entertainment, llm-host, opencode, pi, homebrew
  - opencode: model=just-llms/claude-sonnet-4-6, disabledProviders=[opencode]
  - providers: just-llms (with onePasswordItem baseURL + API key refs), ollama (local models)
  - opencode.commands (diataxis, workspace) → profile
  - opencode.extraMcpServers (github, jira, confluence) → profile
  - claude-code.mcpServers → profile
- [ ] Machine overrides (NOT in profile) preserved from targets/wweaver/default.nix:
  - ollama host = "0.0.0.0", models = [qwen3.5:2b, qwen3.5]
  - vane config (colima: cpu=6, memory=12, disk=60; openaiBaseUrlOpnixItem; defaultModel=qwen3.5; embeddedSearxng=true)
  - homebrew.casks = ["granola"]
  - system.primaryUser = "wweaver"
  - system.stateVersion = 4
  - onepassword.sudoPasswordRef (specific to wweaver work account)
  - skills.externalInputs (vercel-skills)
- [ ] Import chain matches: configuration, nix-homebrew, modules/, modules/roles/homebrew.nix, modules/services/ollama/darwin.nix, modules/services/vane/darwin.nix, os/darwin.nix, modules/home-manager/aerospace.nix, home-manager.darwinModules.home-manager
- [ ] home-manager.sharedModules includes opnix
- [ ] Old darwinConfigurations.wweaver unchanged
- [ ] `nix store diff-closures .#wweaver .#wweaver-v2` is empty or only expected changes
- [ ] `devenv tasks run test:darwin-eval` passes

# Files
- flake.nix: current wweaver at lines 232-246
- targets/wweaver/default.nix: source of truth (168 lines), extract overrides from here
- profiles/wweaver/profile.nix: profile data source — already has user/roles/opencode/providers
- library/archetypes/developer-laptop-darwin.nix: archetype config

# Override vs Profile Boundary
| Data | Location | Reason |
|------|----------|--------|
| user.name, email, fullName | profile | User identity |
| roles | profile | User preference |
| opencode.model, disabledProviders | profile | User preference |
| opencode.commands (diataxis, workspace) | profile | User workflow |
| opencode.extraMcpServers | profile | User services |
| opencode.providers (just-llms, ollama) | profile | User API config |
| claude-code.mcpServers | profile | User services |
| ollama.host, model list | override | Hardware-specific |
| vane colima sizing | override | Hardware-specific |
| homebrew.casks | override | Platform-specific |
| onepassword.sudoPasswordRef | override | Account-specific |
