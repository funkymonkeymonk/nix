# Problem
wweaver is the primary Darwin machine (targets/wweaver/default.nix, 168 lines) and the ABSOLUTE LAST migration per the design doc. Most complex config: work laptop with Justworks provider, Ollama local, Vane with Colima, multiple opencode providers/agents/commands, pi agent. The profile data in targets/wweaver/ already overlaps with profiles/wweaver/profile.nix.

Key tension: wweaver's target config duplicates what's in profiles/wweaver/profile.nix (user, roles, opencode model, providers). The migration should reconcile these — use the profile as the data source, not the target config.

# Acceptance Criteria
- [ ] wweaver-v2 added as parallel darwinConfiguration using library.lib.mkDarwinSystem
- [ ] Uses library/archetypes/developer-laptop-darwin.nix
- [ ] Profile data from profiles/wweaver/profile.nix used as the user/role/provider source:
  - user: wweaver / wweaver@justworks.com / Will Weaver
  - roles: developer, desktop, workstation, entertainment, llm-host, opencode, pi, homebrew
  - opencode model: just-llms/claude-sonnet-4-6, disabledProviders: [opencode]
- [ ] Machine-specific data (NOT in profile) preserved from targets/wweaver/default.nix:
  - ollama host = "0.0.0.0", models = [qwen3.5:2b, qwen3.5]
  - vane config (colima cpu=6, memory=12, disk=60, openaiBaseUrlOpnixItem, defaultModel=qwen3.5)
  - opencode extraMcpServers (github, jira, confluence)
  - opencode commands (diataxis, workspace)
  - opencode providers.just-llms (npm, onePasswordItem, dynamicModels, per-model config "us.anthropic.claude-opus-4-5-20251101-v1:0")
  - opencode ollama provider (local models)
  - claude-code mcpServers
  - pi agent (implicitly enabled via role)
  - homebrew.casks = ["granola"]
  - system.primaryUser = "wweaver"
  - system.stateVersion = 4
  - onepassword.sudoPasswordRef (specific to wweaver work account)
- [ ] Import chain matches: configuration, nix-homebrew, modules/, modules/roles/homebrew.nix, modules/services/ollama/darwin.nix, modules/services/vane/darwin.nix, os/darwin.nix, modules/home-manager/aerospace.nix, home-manager.darwinModules.home-manager
- [ ] home-manager.sharedModules includes opnix
- [ ] Old darwinConfigurations.wweaver unchanged
- [ ] `nix store diff-closures .#wweaver .#wweaver-v2` is empty or only expected changes
- [ ] `devenv tasks run test:darwin-eval` passes

# Files
- flake.nix: current wweaver at lines 232-246
- targets/wweaver/default.nix: source of truth (168 lines), but profile data supersedes
- profiles/wweaver/profile.nix: profile data source
- library/archetypes/developer-laptop-darwin.nix: archetype config

# Notes
- This is the LAST migration before cleanup — requires highest care
- Profile data and target override boundary must be clear:
  - Profile → user info, roles, opencode model, providers with secrets (1Password refs)
  - Target override → hardware sizing (colima cpu/memory/disk), ollama binding, system stateVersion, extra casks
- The just-llms provider has a onePasswordItem — this is an activation-time secret, goes in profile (or override), NOT stored in Nix store
- opencode extraMcpServers and commands are user preference, could go in either profile or override depending on whether they change per-machine
