## Resolution

Closed as a no-op after "Remove dead OpenClaw legacy modules" landed.
legacy.nix's duplicate options.myConfig.openclaw.hardening.* block
(byte-for-byte copy of hardening.nix's options, minus a few) is gone
along with the whole file. hardening.nix is now the single source of
truth for OpenClaw hardening options -- there is nothing left to
deduplicate.

PR: https://github.com/funkymonkeymonk/nix/pull/371
