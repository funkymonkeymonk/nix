## Resolution

Built scripts/generate-architecture-diagram.py which parses the real
static imports lists (not hardcoded) and renders a Mermaid flowchart.
Discovered scripts/docs-update.sh's generate_* functions are entirely
hardcoded/stale (filed separately) -- this new script was deliberately
designed with a staleness self-check (check_manual_edges) to avoid
repeating that mistake for the one part that can't be pure-grepped
(the runtime-conditional role-gated home-manager imports in users.nix).

Embedded the generated diagram in docs/explanation/architecture.md,
also fixed unrelated stale content (fictional development.nix/dnsmasq
examples) noticed while there.

PR: https://github.com/funkymonkeymonk/nix/pull/379
