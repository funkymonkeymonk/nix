# Goal
The opnix review fixes changed platform branching in onepassword.nix but
only the option *types* are tested. The actual config output (which packages
get installed, whether programs._1password is set on NixOS) is not validated.

# Acceptance Criteria
- [ ] Test evaluates onepassword.nix config block with isDarwin=true and isDarwin=false
- [ ] Asserts programs._1password.enable = true on NixOS path
- [ ] Asserts _1password-cli in environment.systemPackages on Darwin path
- [ ] Test passes in CI

# Files
- tests/test-packages.nix (add near existing onepasswordOptionsTest)
- modules/common/onepassword.nix (the module being tested)
