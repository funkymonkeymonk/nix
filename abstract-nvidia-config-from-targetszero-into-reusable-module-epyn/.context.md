# Problem
NVIDIA GPU configuration is hardcoded inline in `targets/zero/default.nix` (lines 38-44). If another NixOS machine with an NVIDIA GPU is added, the config would need to be duplicated. No `modules/nixos/nvidia.nix` exists.

# Acceptance Criteria
- [ ] Create `modules/nixos/nvidia.nix` with a `myConfig.nvidia.enable` option (default: false)
- [ ] The module sets `services.xserver.videoDrivers = ["nvidia"]` and `hardware.nvidia.*` when enabled
- [ ] `targets/zero/default.nix` imports `modules/nixos/nvidia.nix` and sets `myConfig.nvidia.enable = true` instead of inline config
- [ ] `devenv tasks run test:nixos-eval` passes

# Files
- `modules/nixos/nvidia.nix` (new file to create)
- `targets/zero/default.nix` (replace inline NVIDIA block with module enable)
- `modules/common/options.nix` (add `myConfig.nvidia.enable` option)
