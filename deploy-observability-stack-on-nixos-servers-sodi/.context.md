# Goal
Metrics and alerting for NixOS infrastructure.

# Components
- Prometheus: Metrics collection
- Grafana: Dashboards
- Alertmanager: Alert routing
- Node exporter: System metrics

# Acceptance Criteria
- [ ] Prometheus scraping all NixOS hosts
- [ ] Grafana with infrastructure dashboards
- [ ] Alertmanager routing to Matrix/Discord
- [ ] Basic alerts: disk, memory, service down

# Files to modify
- modules/nixos/prometheus.nix (may exist, check)
- modules/nixos/grafana.nix (new or extend)
- modules/nixos/alertmanager.nix (new)

# Notes
Can reuse existing Prometheus from darwin-server or deploy per-host.
Consider federation if scaling to multiple Prometheus instances.

## Needs Refinement

Flagged by autonomous agent on 2026-05-12.

**Reason:** Same issue as 'Deploy log aggregator on NixOS servers' — target configurations are not specified. Additionally, the yak notes 'Consider whether to reuse darwin-server Prometheus or deploy per-host' which is an architecture decision requiring human input. Alertmanager routing to Matrix/Discord requires knowing the Matrix/Discord credentials and endpoints.

To unblock: specify target hosts, Prometheus federation strategy (separate vs reuse darwin), and Matrix/Discord webhook configuration.

## Decision (2026-08-22)

Target host: type-server only. Confirmed by Will 2026-08-23. Consider whether this should reuse/federate with the darwin-server Prometheus instance (see 'Deploy observability stack (Prometheus/Grafana/Loki/Vector) on darwin-server -- native launchd') rather than deploying a second standalone Grafana -- Grafana on darwin-server could add type-server's Prometheus as a second datasource instead of running its own Grafana. Alertmanager routing (Matrix/Discord) still needs credentials -- flag back if not available.
