## Resolution

Extracted the shared nixpkgs.config + base overlay list (previously
duplicated 3x: mkDarwinSystem, mkNixosSystem, and flake.nix's
configuration binding) into library/lib/nixpkgs-config.nix.

Verified via nix eval that all 4 real configs touching either code path
(wweaver, MegamanX, zero, type-nas) produce byte-identical config and
overlay output before/after.

PR: https://github.com/funkymonkeymonk/nix/pull/374
