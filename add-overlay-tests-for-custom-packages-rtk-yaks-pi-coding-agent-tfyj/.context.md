## Resolution

Added tests/test-overlay-packages.nix building rtk, yaks, and
pi-coding-agent via pkgs.runCommand nativeBuildInputs, verifying the
binary is on PATH and --help works. Wired into tests/default.nix,
flake.nix checks, and devenv.nix's test:all curated list.

Verified each check builds and passes individually.

PR: https://github.com/funkymonkeymonk/nix/pull/376
