## Goal
Build a real, working skill vendoring system that pins upstream specifications (agentskills.io, Anthropic best practices, etc.) via flake inputs and makes them available as reference documentation.

## Background
The `writing-skills` skill previously claimed to vendored upstream references (`references/agentskills-spec.mdx`, `references/superpowers-writing-skills.md`, etc.) pinned by sha in `flake.lock`. These files and the flake input never existed — the section was entirely fabricated. The false section has been removed from `writing-skills/SKILL.md`. This yak tracks building the infrastructure to make the vendoring claim real.

## Prerequisites
- Understand how vendored inputs work in this repo's flake
- Decide which upstream specs are worth vendoring vs. linking

## Acceptance Criteria
- [ ] New flake input `agentskills` (or similar) that tracks an upstream spec repo
- [ ] `writing-skills/references/` directory generated at build time from the vendored input
- [ ] Reference files pinned by sha in `flake.lock` and updated via `nix flake lock --update-input`
- [ ] `docs/reference/skill-development.md` created with repo-specific guidelines (expanded from `writing-skills` content)
- [ ] `writing-skills/SKILL.md` updated to reference the real vendored files
- [ ] `devenv tasks run check:lint` passes
- [ ] `devenv tasks run test:build-checks` passes

## Files to change
- `flake.nix` — add new input
- `modules/home-manager/skills/internal/writing-skills/SKILL.md` — update references section
- `modules/home-manager/skills/internal/writing-skills/` — add `references/` generation
- `docs/reference/skill-development.md` — new file

## Notes
Keep the scope focused: the goal is to make vendoring real, not to vendor every possible upstream doc. Start with the most valuable reference (likely agentskills.io spec).

## Needs Refinement

Flagged by autonomous agent on 2026-08-23.

**Reason:** Acceptance criteria require picking a specific upstream spec source ('New flake input agentskills (or similar) that tracks an upstream spec repo') without specifying which repo/URL to vendor. This is a genuine human decision (which upstream spec repo is authoritative/worth pinning), not something an agent should guess.

To unblock: specify the exact upstream repo URL (e.g. a specific agentskills.io spec repo, or Anthropic's published best-practices repo) to vendor.
