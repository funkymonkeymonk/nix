## Resolution

Added pkgs.mergiraf to the developer role. No jj config needed --
jj ships a default merge-tools.mergiraf entry out of the box. Git
merge-driver registration deliberately left out of scope: discovered
programs.git.enable is never set to true anywhere in this repo, so
programs.git.settings (commit signing) never actually applies --
filed as a separate @needs-human yak rather than fixed inline.

Verified: built the package standalone, confirmed the binary runs,
confirmed it resolves in a real config's systemPackages.

PR: https://github.com/funkymonkeymonk/nix/pull/375
