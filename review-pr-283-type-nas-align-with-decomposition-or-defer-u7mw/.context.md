# Goal
Decide whether to merge PR #283 (type-nas config) as-is or defer until after Phase 0
flake decomposition so it can be introduced using the new archetype structure.

# PR Details
PR #283: https://github.com/funkymonkeymonk/nix/pull/283
Adds type-nas NixOS config with ZFS + paperless-ngx (port 28981), sanoid snapshots.

# Decision Needed
Option A: Merge as-is (adds to current monolith, needs rebasing to new structure in Phase 3)
Option B: Defer until after Phase 0 so it uses the new archetype pattern from the start

From the design doc: "nas (platform-agnostic, deferred — added after Phase 0 via PR #283 rebase)"
The design already anticipates a nas archetype added post-Phase 0.

# Acceptance Criteria
- [ ] Human decides: merge now or defer to after Phase 0
- [ ] If deferring: PR closed with note to rebase after Phase 0 completes
- [ ] If merging: PR reviewed and merged, note added to Phase 3 scope

# Action Required
Human decision needed. Recommendation from design doc: defer until after Phase 0.
