# Goal
Relocate the stray yak file from the repo root into the `.yaks/` directory where all other yaks live.

## Prerequisites
- None

# Acceptance Criteria
- [ ] Move `yaks/openclaw-discord-missing-dependency.md` to `.yaks/openclaw-discord-missing-dependency.md`
- [ ] Verify the moved file is picked up by `yx ls`
- [ ] Delete the empty `yaks/` directory
- [ ] Run `devenv tasks run check:lint`

# Files
- `yaks/openclaw-discord-missing-dependency.md` (move to `.yaks/`)
- `yaks/` (delete if empty)

# Notes
This is a trivial janitorial task. All other yak files live under `.yaks/`. The stray file in the root `yaks/` directory is an inconsistency.
