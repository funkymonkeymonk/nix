# Problem
Once all machines are migrated to v2 outputs and verified, remove the old build system and simplify the flake. This is the final phase per the design doc.

# Acceptance Criteria
- [ ] All machines confirmed running on v2 outputs (Phase 2-7 verified via closure diffs and/or actual machine switch)
- [ ] Remove all old nixosConfigurations and darwinConfigurations except:
  - microvm.nixosConfigurations.* (still used)
  - v2 configs (renamed to drop -v2 suffix once verified)
- [ ] Remove targets/ directory (all machine config is now in archetype + override pattern)
- [ ] Remove machine-types/ directory (replaced by library/archetypes/)
- [ ] Remove mkMicrovm helper (replaced by _mkMicrovmV2 or library helper)
- [ ] Remove mkUser helper from flake.nix (replaced by profile data)
- [ ] Simplify flake.nix to flake-parts composition as described in Section 6 of design doc
- [ ] Remove installer-iso nixosConfiguration (or migrate to library helper)
- [ ] `devenv tasks run check:all` passes after cleanup
- [ ] CI pipeline still validates all remaining configs

# Files
- flake.nix: entire file needs simplification
- targets/: entire directory to be removed
- machine-types/: entire directory to be removed
- library/archetypes/: verify this covers all machine-types/ config
- schema/flake-module.nix, library/flake-module.nix, profiles/flake-module.nix: may need wiring into root flake

# Notes
- DO NOT remove microvm.nixosConfigurations until dev-vm-v2 and openclaw-v2 are fixed (deferred from Phase 1)
- installer-iso is a special case — may need to stay as-is or be migrated separately
- The root flake.nix should become: flake-parts.lib.mkFlake { imports = [ ./schema/flake-module.nix ./library/flake-module.nix ./profiles/flake-module.nix ]; }
- microvm.nixosConfigurations may need to live in the library flake-module or a separate module
- packages (rtk, yaks, installer, iso, microvm runners) need to be preserved somewhere
- checks need to be preserved somewhere (currently inline in flake.nix ~429-518)
