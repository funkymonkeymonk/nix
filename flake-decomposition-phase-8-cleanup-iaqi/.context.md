# Goal
Final cleanup once all machines are confirmed on new library-based structure.
Remove all old targets/, machine-types/, and simplify flake.nix to pure flake-parts.

# Reference
Full design: docs/plans/2026-05-11-flake-decomposition-design.md (Section 11, Phase 8)
Requires: Phase 7 complete AND 2 week wweaver monitoring period passed

# Acceptance Criteria
- [ ] targets/ directory removed entirely
- [ ] machine-types/ directory removed entirely
- [ ] flake.nix simplified to pure flake-parts composition (no inline nixosConfigurations/darwinConfigurations)
- [ ] All system outputs now come from library/flake-module.nix
- [ ] devenv tasks run check:all passes
- [ ] No regressions on any machine
- [ ] Final PR merges cleanly onto main

# Files
- flake.nix (major simplification)
- targets/ (remove)
- machine-types/ (remove)
