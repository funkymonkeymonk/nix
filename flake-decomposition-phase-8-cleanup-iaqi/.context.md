# Current scope
The zero flake-parts pilot, Linux evaluation repair, Darwin role cask merge, canonical devenv package sourcing, VM CI coverage, and option ownership migrations are complete or represented by the current PR stack.

# Remaining architecture
- Decide whether a minimal build-only core Darwin target provides unique value.
- Define the generated instance-flake interface and prototype NixOS self-updating instances.
- Normalize any remaining machine/host/target boundaries discovered during that design.

# Current validation note
Linux flake checks, role checks, VM checks, and direct linting pass. Full Darwin all-systems evaluation may still depend on the local/generated devenv environment and upstream Darwin tooling; this is not a cask-merge blocker.
