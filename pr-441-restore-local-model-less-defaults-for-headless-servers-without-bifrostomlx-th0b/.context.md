# Resolution (2026-09-10)

Removed all hard-coded OpenCode model assignments from mkUser, the OpenCode role, Darwin archetypes, and workstation hosts. OpenCode now retains provider discovery configuration and omits the model key so the discovery plugin selects the available model.

# Verification

- mk-user and opencode-bifrost checks pass.
- darwin-server and wweaver evaluate with model = null.
- Generated OpenCode settings omit the model key.
- check:lint passes.
- test:build-checks passes on x86_64-linux.
