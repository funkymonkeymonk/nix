## Why
docs/reference/llm-stack.md's "Target Configuration (MegamanX)" example
(lines ~147-197) is stale: it still shows `qwen3.6-35b` /
`Qwen3.6-35B-A3B-4bit` / `memoryBudgetGb = 32` / a single instance with
`enableContinuousBatching`/`enablePrefixCache` -- none of which matches the
live `hosts/megamanx/default.nix`, which now runs Qwen3.8-27B-8bit with MTP
speculative decoding (SimpleEngine, no batching) on port 8300 plus a second
Gemma 4 e4b BatchedEngine instance on port 8301. The "Available Models" table
(lines ~199-208) is also stale and missing both current models entirely.

## What to do (do this LAST, after the other MegamanX yaks land, since several
of them will change the live config again)
1. Replace the "Target Configuration (MegamanX)" example with the actual
   current `hosts/megamanx/default.nix` vllmMlx + vllmMlxInstances.gemma blocks
   (or a trimmed illustrative version of them)
2. Update "Available Models" table: Qwen3.8-27B-8bit (+ MTP-8bit draft),
   Gemma-4-e4b-it-4bit, and Qwen3.8-27B-4bit (+ MTP-4bit draft) if wweaver's
   install yak has landed by then
3. Update the Engine-modes guidance to reflect the MTP-vs-BatchedEngine
   tradeoff findings from the "Investigate MTP + continuous-batching
   compatibility" yak -- this is genuinely useful operational knowledge that's
   currently undocumented anywhere
4. Add wweaver as a second target in the doc if its install yak has landed,
   noting the deliberate difference in quant choice (4-bit vs 8-bit) driven by
   RAM budget (48GB vs 128GB)
5. Update the "Known Issues & Risks" table with any new findings from the
   KV-cache-precision and MTP-compatibility research yaks

## Acceptance criteria
- [ ] Doc's config example matches the actual current hosts/megamanx/default.nix
      byte-for-byte in intent (same models, ports, budgets, MTP settings)
- [ ] Available Models table lists every model actually served across both
      hosts
- [ ] No remaining references to qwen3.6-35b or memoryBudgetGb=32
