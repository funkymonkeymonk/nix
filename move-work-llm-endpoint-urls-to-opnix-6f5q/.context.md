# Goal
Move internal work endpoint URLs out of the public Nix config and into 1Password
via opnix, so the profile flake and library can be fully public with no corporate
infrastructure naming exposed.

# Affected values
- targets/wweaver/default.nix:48 — vane.openaiBaseUrl = "https://litellm.justworksai.net"
- targets/wweaver/default.nix:125 — opencode.providers.just-llms.baseURL = "https://litellm.justworksai.net"

# Approach
1. Create a 1Password item for the LiteLLM base URL (e.g. op://Personal/LiteLLM/baseURL)
2. Add opnix secret for the URL — write to a file or env var at activation time
3. Update vane and opencode config to read from the opnix-managed path
4. Remove hardcoded URL from targets/wweaver/default.nix

# Acceptance Criteria
- [ ] litellm.justworksai.net no longer appears in any public .nix file
- [ ] vane.openaiBaseUrl reads from opnix-managed secret
- [ ] opencode.providers.just-llms.baseURL reads from opnix-managed secret
- [ ] System activates correctly with opnix providing the value
- [ ] devenv tasks run check:all passes

# Files
- targets/wweaver/default.nix (remove hardcoded URLs)
- modules/services/vane/ (add opnix integration if needed)
