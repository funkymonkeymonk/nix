# Goal
Decompose the `modules/common/options.nix` god file so each option domain lives with the module that uses it.

## Status correction (2026-08-15)
This yak was previously marked done, but the file layout it specified (`modules/common/options/{core,services,agents,system,infra,email,desktop}.nix`) was never created — the file is still a single 898-line monolith and no `modules/common/options/` dir exists in any commit.

The work that actually shipped was PR #361 ("migrate remaining options.nix domains to owning modules"): charm, syncthing, zellij, llmClient, and tailscale options were extracted into their *owning* modules (modules/common/charm.nix, syncthing.nix, zellij.nix, llm-client.nix, modules/roles/tailscale.nix) — a different layout than this yak specified.

## Re-scoped plan
The "owning module declares its own options" pattern from PR #361 is the right one. Remaining work: migrate the domains still declared in `modules/common/options.nix` into their owning modules, then shrink the file to the true cross-cutting core (users, isDarwin, roles.*.enable).

# Acceptance Criteria
- [ ] Audit `modules/common/options.nix` and list every remaining domain still declared there
- [ ] Migrate each remaining domain into its owning module (pattern from PR #361), updating all callers and evalModules-based tests
- [ ] `modules/common/options.nix` reduced to cross-cutting core only
- [ ] `devenv tasks run check:lint` and `devenv tasks run test:build-checks` pass

# Files
- `modules/common/options.nix` (shrink)
- Owning modules per remaining domain (TBD by audit)

# Notes
- Do NOT create the original `modules/common/options/` sub-file layout — PR #361 established the owning-module pattern instead.
- Related yak "migrate-remaining-optionsnix-domains-to-owning-modules" exists under repo-wide cleanup and refactoring; check whether this yak and that one overlap and merge them if so.
