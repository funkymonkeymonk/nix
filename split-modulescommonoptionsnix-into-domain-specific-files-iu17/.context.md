# Goal
Decompose the `modules/common/options.nix` god file so each option domain lives with the module that uses it.

## Status correction (2026-08-15)
This yak was previously marked done, but the file layout it specified (`modules/common/options/{core,services,agents,system,infra,email,desktop}.nix`) was never created — the file is still a single 898-line monolith and no `modules/common/options/` dir exists in any commit.

The work that actually shipped was PR #361 ("migrate remaining options.nix domains to owning modules"): charm, syncthing, zellij, llmClient, and tailscale options were extracted into their *owning* modules (modules/common/charm.nix, syncthing.nix, zellij.nix, llm-client.nix, modules/roles/tailscale.nix) — a different layout than this yak specified. The owning-module pattern is the right one; this yak is re-scoped to finish that migration.

Note: a sibling yak "Migrate remaining options.nix domains to owning modules" exists and is also marked done without context — it likely refers to the same PR #361 work. Consider merging it into this yak to avoid duplicate tracking.

## Audit (2026-08-15): domains still declared in options.nix
Cross-cutting core (keep): `isDarwin`, `users`, `roles` (role enable toggles).
Still to migrate to owning modules: `email-agent`, `email-backup`, `skills`, `opencode`, `fjj`, `sharedModels`, `llmEndpoints`, `claude-code`, `pi`, `sketchybar`, `serviceRegistry`.

# Acceptance Criteria
- [ ] Migrate each remaining domain above into its owning module (pattern from PR #361), updating all callers (targets, machine-types, archetypes, flake.nix overrides) and evalModules-based tests
- [ ] `modules/common/options.nix` reduced to cross-cutting core only (isDarwin, users, roles)
- [ ] `devenv tasks run check:lint` and `devenv tasks run test:build-checks` pass

# Files
- `modules/common/options.nix` (shrink to core)
- Owning modules per domain, e.g. `modules/common/onepassword.nix` pattern; new files for email/skills/opencode/claude-code/pi/sketchybar/serviceRegistry as needed
- `modules/default.nix` (wire new option modules)
- `tests/` (evalModules test lists)

# Notes
- Do NOT create the original `modules/common/options/` sub-file layout — PR #361 established the owning-module pattern instead.
- Suggest renaming this yak to "Finish migrating options.nix domains to owning modules" to match what it actually is.
