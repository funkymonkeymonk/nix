# Goal
Decompose the 600+ line `modules/common/options.nix` god file into domain-specific option modules under `modules/common/options/`.

## Prerequisites
- None

# Acceptance Criteria
- [ ] Create `modules/common/options/` directory
- [ ] Create `modules/common/options/core.nix` — users, isDarwin, roles.*.enable, agent-skills
- [ ] Create `modules/common/options/services.nix` — bifrost, vane, caddy, searxng, lume
- [ ] Create `modules/common/options/agents.nix` — opencode, claude-code, pi, llmClient
- [ ] Create `modules/common/options/system.nix` — onepassword, syncthing, charm, zellij, fjj, jj-autosync
- [ ] Create `modules/common/options/infra.nix` — microvm, tailscale, autoUpgrade, motd, serviceRegistry
- [ ] Create `modules/common/options/email.nix` — email-agent, email-backup
- [ ] Create `modules/common/options/desktop.nix` — aerospace, sketchybar, vllmMlx
- [ ] Update `modules/common/options.nix` to import all sub-modules via `imports`
- [ ] Ensure all existing tests pass without modification (backward compatibility)
- [ ] Run `devenv tasks run check:lint` and `devenv tasks run test`

# Files
- `modules/common/options.nix` (refactored to imports-only)
- `modules/common/options/core.nix` (new)
- `modules/common/options/services.nix` (new)
- `modules/common/options/agents.nix` (new)
- `modules/common/options/system.nix` (new)
- `modules/common/options/infra.nix` (new)
- `modules/common/options/email.nix` (new)
- `modules/common/options/desktop.nix` (new)

# Notes
The current file mixes options for 15+ domains. Splitting improves:
- Code review (smaller diffs)
- Merge conflict reduction
- Discoverability (new contributors can find options by domain)
- The `serviceRegistry` port-conflict assertion at the bottom of the file should move to `infra.nix`.
