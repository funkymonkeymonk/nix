# Problem
zero is a real NixOS desktop machine (targets/zero/default.nix). It currently uses the old nixpkgs.lib.nixosSystem pattern with machine-types/desktop.nix. Needs a parallel v2 output using library.lib.mkNixosSystem + library/archetypes/desktop-nixos.nix.

This is the first real-machine migration, so closure diff matters more than Phase 2 (which were templates). zero has hardware-specific config (NVIDIA GPU, sleep disable, Discord package, custom firewall) that must be preserved.

# Acceptance Criteria
- [ ] zero-v2 added as parallel nixosConfiguration in flake.nix using library.lib.mkNixosSystem
- [ ] Uses library/archetypes/desktop-nixos.nix
- [ ] Machine-specific config preserved from targets/zero/default.nix:
  - NVIDIA GPU (nvidia.open=false, stable driver)
  - Sleep disabled (systemd.sleep.settings)
  - Discord package
  - Firewall disabled
  - llmEndpoints pointing to MegamanX
  - autoUpgrade.flakeUrl = "github:funkymonkeymonk/nix#zero-v2"
  - myConfig from mkUser "monkey" + role enables (developer, desktop, opencode, tailscale)
- [ ] Import chain matches old output: hardware-configuration, ./modules/nixos/{base,desktop,gaming,streaming}.nix, ./modules/services/ollama/nixos.nix, ./modules/services/openclaw, inputs.nix-openclaw.nixosModules.openclaw-gateway, os/nixos.nix, inputs.disko.nixosModules.disko, disk-configs/single-disk-ext4.nix
- [ ] Old nixosConfigurations.zero unchanged (parallel output pattern)
- [ ] `nix store diff-closures .#zero .#zero-v2` is empty or only expected changes
- [ ] `devenv tasks run test:nixos-eval` passes

# Files
- flake.nix: add zero-v2 nixosConfiguration (current zero config at lines ~321-355)
- targets/zero/default.nix: source of truth for machine-specific config
- library/archetypes/desktop-nixos.nix: compare against machine-types/desktop.nix for parity
- library/lib/mk-system.nix: note that mkNixosSystem includes the root modules/ import, so modules/nixos/*.nix imports must be in the modules list

# Notes
- mkNixosSystem already includes: root modules/, home-manager, opnix, nixpkgs config, overlays
- Specifically NOT included by mkNixosSystem: nixos/base.nix, nixos/desktop.nix, nixos/gaming.nix, nixos/streaming.nix, ollama/nixos.nix, openclaw module, nix-openclaw, os/nixos.nix, disko, disk-configs — these must be in the modules list
- The hardware-configuration import pattern (pathExists vs stub) should be replicated
- home-manager.sharedModules needs nix-openclaw's openclaw module (same as current)
