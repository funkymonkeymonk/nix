# Goal
Migrate zero (NixOS desktop) to library. First real machine with state — use parallel
output and closure diff gate before switching.

# Reference
Full design: docs/plans/2026-05-11-flake-decomposition-design.md (Section 11, Phase 3)
Requires: Phase 2 complete

# Acceptance Criteria
- [ ] nixosConfigurations.zero-v2 added using mkNixosSystem
- [ ] machines/zero.nix created in inventory style (platform, archetype, profile ref, hostname, overrides)
- [ ] nix store diff-closures .#nixosConfigurations.zero .#nixosConfigurations.zero-v2 is clean
- [ ] zero switched to use v2 config
- [ ] Old nixosConfigurations.zero removed, zero-v2 renamed to zero
- [ ] targets/zero/ removed
- [ ] devenv tasks run check:all passes
- [ ] Stacked PR created on top of Phase 2 PR

# Files
- flake.nix (replace zero entry)
- targets/zero/ (remove)
