# Decision: MERGE AS-IS

Protoman is a darwin-server instance — same headless-server-darwin archetype.
Deploy-rs is a temporary workaround because nix-darwin system.autoUpgrade doesn't
exist yet (blocked on nix-darwin PR #1682).

When PR #1682 lands:
- Remove deploy-rs from protoman config
- Add autoUpgrade.flakeUrl to protoman
- Protoman moves to pull-based auto-upgrade like all other Darwin machines

# Acceptance Criteria
- [ ] PR #284 reviewed and merged
- [ ] Note added to PR/commit that deploy-rs is temporary pending nix-darwin #1682
- [ ] Protoman added to Phase 4 (darwin migration) scope
