# Problem
`modules/common/options.nix` has 4 uses of untyped `types.attrs` (the Nix equivalent of `any`):
- Line 681: `myConfig.mods.settings` — arbitrary mods config YAML
- Line 1035: `myConfig.claude.extraSettings` — arbitrary Claude Code settings
- Line 1105: `myConfig.pi.settings` — arbitrary pi-mono settings
- Line 1212: `myConfig.sketchybar.themes` — `types.attrsOf types.attrs` (nested untyped)

These give no type checking or documentation of what keys are valid.

# Acceptance Criteria
- [ ] Each `types.attrs` is replaced with a more specific type where possible:
  - For pass-through JSON/YAML config blobs (mods, claude, pi): use `types.attrsOf types.anything` (still flexible but documents intent)
  - For `themes`: use `types.attrsOf (types.attrsOf types.anything)` 
- [ ] `devenv tasks run test:nixos-eval` and `test:darwin-eval` pass
- [ ] No existing machine configs break

# Files
- `modules/common/options.nix` (lines 681, 1035, 1105, 1212)
