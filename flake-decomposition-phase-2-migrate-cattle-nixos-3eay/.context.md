# Problem
The flake.nix still defines type-server, type-server-arm, and type-desktop using the old nixpkgs.lib.nixosSystem pattern with machine-types/*.nix. These need parallel v2 outputs using library.lib.mkNixosSystem + library/archetypes/*.nix, following the Phase 1 microvm pattern.

These are template configs (no specific real machines running them), so the migration is low-risk.

# Acceptance Criteria
- [ ] type-server-v2, type-server-arm-v2, and type-desktop-v2 added as parallel nixosConfigurations in flake.nix
- [ ] Each uses library.lib.mkNixosSystem with library/archetypes/*.nix
- [ ] Machine-specific config (SSH keys, hostname, autoUpgrade flakeUrl) passed as overrides or inline modules
- [ ] Old nixosConfigurations unchanged (parallel output pattern)
- [ ] `nix store diff-closures .#type-server .#type-server-v2` is empty or contains only expected changes
- [ ] Same closure diff verified for type-server-arm and type-desktop
- [ ] `devenv tasks run test:nixos-eval` passes

# Files
- flake.nix: add v2 nixosConfigurations (lines ~305-418)
- library/lib/mk-system.nix: may need adjustment for inputs passing
- library/archetypes/headless-server-nixos.nix: verify it covers all machine-types/server.nix config
- library/archetypes/desktop-nixos.nix: verify it covers all machine-types/desktop.nix config
- machine-types/server.nix: source of truth to match (autoUpgrade.flakeUrl, SSH keys, etc.)
- machine-types/server-arm.nix: same for ARM variant
- machine-types/desktop.nix: same for desktop

# Notes
- The existing _mkMicrovmV2 helper is NOT the pattern to follow — use library.lib.mkNixosSystem directly
- mkNixosSystem already includes home-manager and opnix modules
- Overrides for autoUpgrade.flakeUrl must be machine-specific (e.g., "github:funkymonkeymonk/nix#type-server-v2")
- SSH authorized keys belong in machine config, not archetype
