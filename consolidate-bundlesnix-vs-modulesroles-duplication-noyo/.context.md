# Problem
`bundles.nix` at the repo root is dead code — it's not imported anywhere.
It was the original approach for role definitions (packages + config per role).
`modules/roles/*.nix` is the current approach using proper NixOS modules.

The duplication is historical: bundles.nix exists but is completely unused.
Only reference: a comment in targets/MegamanX/default.nix:145.

# Acceptance Criteria
- [ ] `bundles.nix` removed from repo root
- [ ] Comment in targets/MegamanX/default.nix:145 removed or updated
- [ ] All existing tests pass (bundles.nix had no importers)
- [ ] `devenv tasks run check:all` passes

# Files
- `bundles.nix` (delete this file)
- `targets/MegamanX/default.nix` (remove line 145 comment referencing bundles.nix)
