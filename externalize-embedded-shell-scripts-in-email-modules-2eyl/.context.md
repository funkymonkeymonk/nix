## Resolution

Extracted all 5 heredoc scripts (email-filters, email-backup-setup,
email-backup, email-backup-restore, email-backup-status) into external
.sh files following the watch-ci-jobs.nix readFile pattern.
Also deduplicated the notmuch config into a single binding used by
both the setup script and the standalone home.file.

Verified byte-for-byte identical output for all 5 scripts + notmuch
config via nix eval diff before/after. All scripts pass shellcheck
with zero warnings.

PR: https://github.com/funkymonkeymonk/nix/pull/369
