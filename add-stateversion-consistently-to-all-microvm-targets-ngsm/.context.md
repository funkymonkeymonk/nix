# Problem
Three of four microvm targets have `system.stateVersion = "25.05"` set, but
`targets/microvms/dev-vm.nix` is missing it entirely.

The `stateVersion` setting tells NixOS which version semantics to use for
stateful options. Missing it causes a warning and can cause subtle upgrade issues.

# Acceptance Criteria
- [ ] `system.stateVersion = "25.05"` is present in `targets/microvms/dev-vm.nix`
- [ ] All four microvm targets have the same stateVersion
- [ ] `devenv tasks run test:nixos-eval` passes

# Files
- targets/microvms/dev-vm.nix (add stateVersion)
- targets/microvms/openclaw.nix (has it: "25.05", reference)
- targets/microvms/matrix.nix (has it: "25.05", reference)
- targets/microvms/media-center.nix (has it: "25.05", reference)
