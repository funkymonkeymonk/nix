# Goal
os/nixos.nix sets boot.loader.systemd-boot.enable = true and
boot.loader.efi.canTouchEfiVariables = true without lib.mkDefault,
which prevents per-machine targets from overriding these settings.

# Acceptance Criteria
- [ ] boot.loader.systemd-boot.enable = lib.mkDefault true
- [ ] boot.loader.efi.canTouchEfiVariables = lib.mkDefault true
- [ ] Machine targets that need different boot settings can override without conflicts
- [ ] check:lint and test:nixos-eval pass

# Files
- os/nixos.nix (lines 3-5, add lib.mkDefault to boot.loader settings)
