## Resolution

Added pkgs.jj-hooks to home.packages in modules/home-manager/foundation.nix
(applies to all users via the foundation home-manager module). Also wired
up the `jj push` alias to delegate to `jj-hp push` and enabled
jj-hooks.advance-bookmarks, completing the parent story's goal.

Verified:
- `devenv tasks run check:lint` passes
- `devenv tasks run test` passes (all foundation checks)
- `nix eval .#darwinConfigurations.wweaver...home.packages` confirms jj-hooks present
- jj-hp/jj-hooks binaries build and run correctly for aarch64-darwin

## Files
- modules/home-manager/foundation.nix
