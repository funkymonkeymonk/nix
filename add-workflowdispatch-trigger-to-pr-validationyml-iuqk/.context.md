# Goal
Add workflow_dispatch trigger to pr-validation.yml so it can be run manually from GitHub UI.

# Acceptance Criteria
- [ ] workflow_dispatch: added to the on: block in .github/workflows/pr-validation.yml
- [ ] Existing pull_request trigger preserved
- [ ] Workflow file parses correctly (yamllint passes)

# Files
- .github/workflows/pr-validation.yml
