# Goal
Migrate minimal recovery configs (core, bootstrap) to library. Low risk — minimal configs,
no daily use.

# Reference
Full design: docs/plans/2026-05-11-flake-decomposition-design.md (Section 11, Phase 5)
Requires: Phase 4 complete

# Acceptance Criteria
- [ ] darwinConfigurations.core migrated or confirmed intentionally minimal (may stay as-is)
- [ ] nixosConfigurations.bootstrap migrated or confirmed intentionally minimal
- [ ] installer-iso output confirmed still works (not a machine, just artifact)
- [ ] targets/core/, targets/bootstrap/ removed if migrated
- [ ] devenv tasks run check:all passes
- [ ] Stacked PR created on top of Phase 4 PR

# Note
core and bootstrap are intentionally minimal recovery configs. They may warrant staying
as simple direct configs rather than going through mkDarwinSystem/mkNixosSystem.
Use judgement — the goal is clarity, not mechanical migration.

# Files
- flake.nix (core, bootstrap, installer-iso entries)
- targets/core/, targets/bootstrap/ (remove if migrated)
