# Current state as of 2026-09-10

VM integration coverage is implemented on main. The original NixOS VM tests are present from #206, role/package VM generation is present from #421, and pull-request CI runs the NixOS VM checks from #465.

# Remaining work

No implementation remains for this yak. Future VM coverage additions should be opened as separate scoped yaks.
