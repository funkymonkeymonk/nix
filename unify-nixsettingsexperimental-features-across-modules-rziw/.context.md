# Goal
Consolidate duplicate nix.settings.experimental-features = ["nix-command" "flakes"] definitions
spread across multiple files into a single shared location (likely os/nixos.nix or a common module).

# Acceptance Criteria
- [ ] nix.settings.experimental-features defined in one canonical location
- [ ] Duplicate definitions removed from: targets/bootstrap/default.nix, targets/installer-iso/default.nix, machine-types/server.nix, machine-types/desktop.nix, scripts/lib/bootstrap-config.nix, os/microvm.nix
- [ ] os/nixos.nix already has it — verify it's the canonical location
- [ ] check:lint and test:nixos-eval pass

# Files
- os/nixos.nix (canonical location — already has it)
- targets/bootstrap/default.nix
- targets/installer-iso/default.nix
- machine-types/server.nix
- machine-types/desktop.nix
- scripts/lib/bootstrap-config.nix
- os/microvm.nix
