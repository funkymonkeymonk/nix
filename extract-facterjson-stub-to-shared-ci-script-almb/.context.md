# Problem
The facter.json stub (a fake hardware description for NixOS CI builds) is copy-pasted in three places:
- `.github/workflows/pr-validation.yml` line 101 (NixOS eval job)
- `.github/workflows/pr-validation.yml` line 303 (NixOS build job)
- `.github/workflows/main-build.yml` line 82

The JSON content is identical in all three. If the stub format ever needs updating (e.g., to add a new hardware field), it must be changed in all three places.

# Acceptance Criteria
- [ ] The facter.json stub content lives in exactly one place (composite action or shared shell script at e.g. `.github/scripts/create-facter-stub.sh` or `.github/actions/facter-stub/`)
- [ ] All three step references are replaced with a single call to the shared source
- [ ] CI still passes (NixOS eval and build jobs succeed)

# Files
- `.github/workflows/pr-validation.yml` (replace stub at lines ~101 and ~303)
- `.github/workflows/main-build.yml` (replace stub at line ~82)
- `.github/scripts/create-facter-stub.sh` or `.github/actions/facter-stub/action.yml` (new file to create)
