# Goal
The workspace detection logic in devenv.nix and .envrc is pure shell
with zero tests. While lower risk (only affects developer UX), it has
complex path-resolution logic and a duplicate implementation.

# Acceptance Criteria
- [ ] Test that _detect_jj_repo_root correctly identifies workspace vs non-workspace
- [ ] Test that functions (s, switch, b, q) are defined in workspace context
- [ ] Test that aliases are used in non-workspace context
- [ ] Consider: consolidate .envrc and devenv.nix implementations to reduce duplication

# Notes
- Hardest to test since it's shell functions in devenv context
- Could use a simple bash test script with mock .jj/repo file
- Lower priority than the Nix module tests

# Files
- devenv.nix (lines 53-101)
- .envrc (lines 17-88)
