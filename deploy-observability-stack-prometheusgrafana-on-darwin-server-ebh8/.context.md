# Goal
Set up metrics collection and visualization on protoman.

# Components
- Prometheus: Metrics collection and storage
- Grafana: Visualization dashboards
- Node exporter: System metrics

# Acceptance Criteria
- [ ] Prometheus running in Colima VM
- [ ] Grafana running in Colima VM with dashboards
- [ ] Node exporter on darwin-server exporting metrics
- [ ] Prometheus scraping MicroVMs and host
- [ ] Alert rules for critical services (Connect, MicroVMs)

# Files to modify
- modules/services/prometheus/darwin.nix (new)
- modules/services/grafana/darwin.nix (new)
- targets/darwin-server/default.nix (enable services)

# Notes
Expose Prometheus on internal network only (192.168.83.0/24).
Grafana should be accessible from Tailscale network.

## Needs Refinement

Flagged by autonomous agent on 2026-05-12.

**Reason:** Same issue as 'Deploy log aggregator on darwin-server' — acceptance criteria require running Prometheus/Grafana in Colima VM which cannot be validated in CI. This conflicts on targets/darwin-server/default.nix with the log aggregator yak. Additionally, 'Alert rules for critical services' and 'Grafana accessible from Tailscale network' are integration concerns beyond code review.

To unblock: either (a) add integration test infrastructure, or (b) accept module code without full integration testing, and test manually on protoman after deployment.
