# Current state as of 2026-09-10

Existing VM tests are wired into the flake checks through tests/default.nix. The suite includes user, SSH, package, foundation-role, and developer-role boot tests, with structural generator coverage on non-Linux hosts. PR validation invokes the NixOS VM checks.

# Acceptance result

- VM tests exported: yes.
- Role VM tests exported: yes.
- CI execution on pull requests: yes via #465.
- Remaining implementation: none.
