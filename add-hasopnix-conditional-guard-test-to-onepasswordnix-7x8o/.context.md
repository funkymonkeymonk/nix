# Goal
The key improvement in the opnix review was the hasOpnix guard that
conditionally applies services.onepassword-secrets config only when the
opnix NixOS module is present. This guard is untested.

# Acceptance Criteria
- [ ] Test evaluates onepassword.nix with opnix module absent (simulating Darwin)
- [ ] Verifies no services.onepassword-secrets config is produced
- [ ] Test evaluates with opnix module present
- [ ] Verifies services.onepassword-secrets config IS produced when secrets are defined
- [ ] No infinite recursion in either case

# Files
- tests/test-packages.nix
- modules/common/onepassword.nix (hasOpnix at ~line 12)
