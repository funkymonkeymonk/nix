# Goal
pi errors with "Stream ended without finish_reason" on tool calls via vllm-mlx + gemma4-31b.

# Root cause (verified)
Engine's terminal output (finished=True, bare <turn|>) is swallowed by a parser `continue`
in stream_chat_completion, so no SSE chunk carries finish_reason. NON-DETERMINISTIC on
current main build: bites when the model keeps its thought channel open through the tool
call (terminal delta travels the reasoning path into the tool parser). Live daemon already
runs the main build c6hrmppq and still fails — a plain update does NOT fix it.

# Upstream status (checked 2026-08-02)
- v0.4.0 (latest release) and upstream main: no fix.
- #628/#629 (open): same symptom, different mechanism (engine natural-stop epilogue).
  Does not cover parser swallows.
- #653/#663 (flush reasoning parser at stream end): closed UNMERGED (CI breakage).
  Narrower than our guard (reasoning parser only).
- No issue covers the tool-call terminal swallow; candidate for upstream submission.

# Status: PR OPEN, awaiting human merge
- PR: https://github.com/funkymonkeymonk/nix/pull/383
- After merge: user must run `s` (system:switch) on megamanx.

# Acceptance Criteria
- [x] Package patch emits terminal finish_reason chunk when finished output was suppressed
- [x] Regression test (RED verified unpatched; GREEN patched)
- [x] devenv tasks run check:lint passes
- [x] Live verification: replay against patched server returns finish_reason="tool_calls"
- [x] PR opened; system:switch NOT run
