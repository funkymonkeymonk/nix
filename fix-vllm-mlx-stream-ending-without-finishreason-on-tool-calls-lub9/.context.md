# Goal
pi errors with "Stream ended without finish_reason" on tool calls via vllm-mlx + gemma4-31b.
Root cause (reproduced with curl replay of the exact failing request direct against :8300):
in vllm_mlx/server.py stream_chat_completion, the engine's terminal output (finished=True,
bare <turn|> end-of-turn token after a completed tool call) is swallowed by the tool
parser's `continue`, so no SSE chunk ever carries finish_reason.

# Status: PR OPEN, awaiting human merge
- PR: https://github.com/funkymonkeymonk/nix/pull/383
- After merge: user must run `s` (system:switch) on megamanx to restart the daemon with
  the patched package.
- Note: main already fixes the reasoning-parser flavor via the <turn|> strip postPatch;
  this PR adds the general guard + regression test (plain tool-parser branch was still
  broken on main).
- Consider filing upstream: swallow class affects all tool parsers, not just gemma4.

# Acceptance Criteria
- [x] Package carries a patch that emits a terminal finish_reason chunk when the engine's
      finished output was suppressed by a parser (tool_calls if detected, else stop/length)
- [x] Regression test in tests/ proving a finished-but-swallowed stream still ends with a
      finish_reason chunk (RED verified: finish_reasons=[] unpatched; GREEN with patch)
- [x] devenv tasks run check:lint passes
- [x] Live verification: replay payload against patched server on scratch port returns
      finish_reason="tool_calls" before [DONE]
- [x] PR opened; system:switch NOT run (user applies)
