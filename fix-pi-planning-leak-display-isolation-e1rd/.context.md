## Resolution (2026-08-22)

Closed as out-of-scope for this repo. 'Display isolation' reads as a pi-tui rendering bug (planning/reasoning content bleeding into the main output pane) -- that's entirely inside pi's own upstream TUI rendering pipeline (earendil-works/pi: pi-tui, coding-agent), which this repo never touches (only packages the prebuilt CLI tarball and writes static config). Its only child, 'Fix Model Token Sampling Collapse', was closed with the same reasoning. File against https://github.com/earendil-works/pi instead.
