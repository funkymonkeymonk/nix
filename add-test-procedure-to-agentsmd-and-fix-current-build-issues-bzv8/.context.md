# Current state as of 2026-09-10

The requested testing procedure is documented in AGENTS.md. Current devenv tasks use `nix flake check` for build checks, nix-unit is included in flake checks, and there is no active test:build-packages task in devenv.nix. The CI/build cleanup children are complete on main.
