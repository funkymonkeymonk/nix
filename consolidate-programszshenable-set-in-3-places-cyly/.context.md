# Problem
`programs.zsh.enable = true;` is set in three places:
- modules/roles/foundation.nix:57
- modules/common/shell.nix:14
- modules/common/core.nix:16

This is redundant. Nix's module system merges these, so having it in three places
is harmless but confusing for maintainability.

# Acceptance Criteria
- [ ] `programs.zsh.enable = true;` exists in exactly one location
- [ ] The canonical location is modules/common/shell.nix (it's the zsh-specific module)
- [ ] The duplicate lines are removed from foundation.nix and core.nix
- [ ] All eval tests pass after removal: `devenv tasks run test:darwin-eval && devenv tasks run test:nixos-eval`
- [ ] No behavior change: zsh still enables on all systems

# Files
- modules/roles/foundation.nix (remove line 57)
- modules/common/shell.nix (keep, canonical location)
- modules/common/core.nix (remove line 16)
