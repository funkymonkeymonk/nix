# Goal\nDecide whether the repository benefits from a minimal darwinConfigurations.core smoke-test target after devenv package sourcing is canonical.\n\n# Constraints\n- Build/eval-only; never a real deployment target.\n- Must not require secrets, Home Manager, or a live 1Password session during evaluation/build.\n- Avoid recreating the historical core-v2 scratch target.\n\n# Decision\nCompare the value of a dedicated Darwin evaluation target against a focused perSystem/package test. Keep the target only if it provides unique architectural or regression value.