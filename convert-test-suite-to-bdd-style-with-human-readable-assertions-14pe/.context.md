## Resolution

Published the BDD style guide (decided 2026-04-07) as a durable section
in docs/explanation/testing.md rather than only living in yak context,
so future contributors can find it without digging through the yak
backlog. Confirmed the decision scope (new tests only, no retroactive
conversion) is preserved verbatim.

Also fixed two remaining bare `devenv tasks run test` references in the
same file (should be `test:all`) discovered while editing it.

Added as a follow-up commit to the already-open docs drift PR since it
touches the exact same file.

PR: https://github.com/funkymonkeymonk/nix/pull/373
