# Goal
Remove the unused perform_installation() function from targets/installer-iso/installer.nix.
The function is defined but never called — it is dead code.

# Acceptance Criteria
- [ ] perform_installation() function body removed from targets/installer-iso/installer.nix
- [ ] No other references to perform_installation remain (grep confirms)
- [ ] The installer script still works (check:lint passes, no syntax errors)

# Files
- targets/installer-iso/installer.nix (remove the perform_installation() function at line ~301)
