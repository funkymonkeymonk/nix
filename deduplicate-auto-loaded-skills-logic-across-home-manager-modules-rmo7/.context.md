## Resolution

Added `mkAutoLoadSkills` to modules/home-manager/lib.nix and refactored
both claude-code.nix and pi-coding-agent.nix to call it instead of
duplicating the manifest-filter-concatenate logic.

Verified byte-for-byte identical .pi/agent/AGENTS.md output before/after
via nix eval diff on the wweaver darwin config.

PR: https://github.com/funkymonkeymonk/nix/pull/368
