## Context
modules/common/scripts/nix-cloud-init offers "Enable LLM host (oMLX)?" at install time and appends "llm-host" to the roles list. The only LLM host role that ever existed ("llm-server") was removed from roles/bundles.nix (PR #432 cleanup); no "llm-host" role exists. Confirming it adds a role that silently does nothing (or errors if roles are validated) — a dead prompt left over from the Ollama era.

## Prerequisites
- PR #441 merged (removes the Ollama-era stack this prompt describes)

## Files
- modules/common/scripts/nix-cloud-init (replace or remove the "llm-host" prompt; if kept, point at a real role such as "omlx" — decide during implementation)
- If the role list is validated elsewhere (roles/bundles.nix, tests), update accordingly
- tests/ if any check covers nix-cloud-init role prompts (search tests/ for "llm-host")

## Acceptance criteria
- [ ] nix-cloud-init no longer offers a role that does not exist in roles/bundles.nix
- [ ] If still offering an LLM prompt: it maps to a real, implemented role
- [ ] No remaining references to "llm-host"/"llm-server" in scripts/ or tests/
- [ ] Odd-eval build of the package containing nix-cloud-init passes (nix build .#checks.x86_64-linux.* plus any nixosConfigurations eval covering this script)