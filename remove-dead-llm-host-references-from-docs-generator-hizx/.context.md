# Goal

Remove obsolete llm-host and llm-server role names from scripts/docs-update.sh and stale generated/planning metadata, or replace the generator fallback with roles discovered from modules/roles.

# Acceptance criteria

- docs-update.sh emits only roles that exist in modules/roles/.
- No generated documentation contains llm-host or llm-server.
- Any stale PRD entries are removed or explicitly archived.
- Documentation generation and validation pass.

# Evidence

Current role implementation removed the old LLM host role in main (#420), but dead references remain in scripts/docs-update.sh and prd.json.
