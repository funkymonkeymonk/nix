# Goal
Remove the empty llm-server bundle from bundles.nix — it has no packages and empty config.

# Acceptance Criteria
- [ ] llm-server bundle removed from bundles.nix (the block with packages = [] and config = {})
- [ ] No references to llm-server bundle elsewhere (grep confirms)
- [ ] check:lint and test:darwin-eval pass

# Files
- bundles.nix (remove the llm-server = { packages = []; config = {}; }; block)
