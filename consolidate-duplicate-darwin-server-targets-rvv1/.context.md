# Goal
Merge `targets/darwin-server/default.nix` and `targets/type-darwin-server/default.nix` which share ~80% of their content.

## Prerequisites
- None

# Acceptance Criteria
- [ ] Compare both files and identify the delta (type-darwin-server lacks OpenClaw-specific home-manager config, cloud-init, and fix-openclaw-deps launchd agents)
- [ ] Decide on canonical target: `targets/darwin-server/default.nix` is the production config; `targets/type-darwin-server/default.nix` is the generic/template version
- [ ] Extract shared config into `targets/darwin-server/common.nix` or similar
- [ ] Refactor both targets to import the shared config and apply deltas
- [ ] Alternatively: delete `targets/type-darwin-server/` if it's unused, and document that `targets/darwin-server/` is the canonical target
- [ ] Update `flake.nix` if any configurations reference the deleted target
- [ ] Run `devenv tasks run check:lint` and `devenv tasks run test`

# Files
- `targets/darwin-server/default.nix`
- `targets/type-darwin-server/default.nix`
- `targets/darwin-server/common.nix` (new, if extracted)
- `flake.nix`

# Notes
Both files define:
- Same `nixpkgs.hostPlatform`, `system.stateVersion`, `system.primaryUser`
- Same `mkUser "monkey" "me@willweaver.dev"`
- Same `skills.superpowersPath`, `roles.developer`, `roles.opencode`
- Same `opencode.enable = true; model = null`
- Same `llmClient.rtk.enable = true`
- Same `lume` configuration
- Same SSH authorized keys
`targets/darwin-server/default.nix` adds OpenClaw home-manager config, cloud-init, and log rotation. `type-darwin-server` adds `security.sudo.extraConfig` and VM helper scripts.
