## Resolution

Implemented via two new lib.nix helpers (mkFullSkillDirs, mkSkillCommands)
consumed by opencode.nix, claude-code.nix, and pi-coding-agent.nix.
Discovered and fixed a real "Conflicting managed target files" bug along
the way: install.nix had to be stripped down to external-skills-only,
since all three agent modules now handle internal+superpowers skills
natively (opencode/claude via programs.<agent>.skills, pi via home.file
since there's no native module upstream).

Verified with a REAL build (not just eval) of a standalone
homeManagerConfiguration with both opencode.nix and claude-code.nix
active: 22 real skill directories + 9 bundled commands materialized
correctly in /nix/store, content verified byte-for-byte against source.

Confirmed zero failed assertions on all 5 real machine configs with
home-manager users (wweaver, darwin-server, MegamanX, zero, zero-v2).

Filed a follow-up yak for an unrelated pre-existing bug found during
this work: 'Fix myConfig.opencode.enable never set to true on wweaver
despite roles.opencode.enable' (kept out of scope for this PR).

PR: https://github.com/funkymonkeymonk/nix/pull/372
