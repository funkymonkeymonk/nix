# Dependency

Blocked by the self-updating instance prototype.

# Scope

Move any machine/host/target boundary violations discovered during the prototype into the appropriate modules, targets, hosts, or archetypes without behavior changes.

# Acceptance criteria

- Every moved setting has a clear owning layer.
- Existing output names and deployment behavior remain unchanged.
- Structural tests document the resulting boundary.
