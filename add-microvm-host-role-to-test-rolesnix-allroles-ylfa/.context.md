# Goal
The microvm-host role exists in options.nix (line ~136) and roles/default.nix
but is completely missing from the allRoles list in test-roles.nix.
This means no eval test runs for this role.

# Acceptance Criteria
- [ ] microvm-host appears in allRoles list in tests/test-roles.nix
- [ ] devenv tasks run test:roles passes with it included
- [ ] The role evaluates without error on both platforms

# Files
- tests/test-roles.nix (add to allRoles around line 110-123)
- modules/roles/microvm-host.nix (the role being tested)
- modules/common/options.nix (role option definition ~line 136)
