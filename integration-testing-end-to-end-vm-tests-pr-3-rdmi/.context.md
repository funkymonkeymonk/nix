
## Note (2026-07-26)
Marked @needs-human: this story requires booting real QEMU/KVM NixOS
VMs (pkgs.testers.nixosTest) to verify service startup, file
installation, and boot behavior. The current dev environment is
aarch64-darwin, which cannot run x86_64-linux VM tests directly --
only eval-level verification is possible here, which isn't sufficient
for a "boots and behaves correctly" claim. Needs a Linux
(x86_64-linux) machine or CI runner to implement and verify properly.
