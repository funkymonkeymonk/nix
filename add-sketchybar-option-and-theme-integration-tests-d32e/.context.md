# Goal
The sketchybar module has zero tests. Need option definition tests
and theme integration validation.

# Acceptance Criteria
- [ ] Test validates all sketchybar options (enable, height, padding, groupPadding, useAerospaceIntegration, extraConfig)
- [ ] Test validates themes.nix exports sketchybarTheme with expected structure
- [ ] Test validates toSketchybarColor hex conversion ("0xRRGGBB" format)
- [ ] Test validates the module is a no-op when isDarwin=false
- [ ] Tests pass in CI

# Files
- tests/test-packages.nix (or new test-sketchybar.nix)
- modules/home-manager/sketchybar/default.nix
- modules/home-manager/themes.nix
- modules/common/options.nix (sketchybar options ~line 1128-1164)
