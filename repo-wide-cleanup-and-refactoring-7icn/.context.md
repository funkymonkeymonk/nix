# Goal
Systematic cleanup of dead code, DRY violations, SOLID principle breaches, and test coverage gaps identified in the comprehensive repo review.

## Prerequisites
- None (this is a story yak; work the child tasks)

# Acceptance Criteria
- [ ] All P0 child yaks are completed (dead code removal)
- [ ] All P1 child yaks are completed (DRY refactoring)
- [ ] All P2 child yaks are completed (pattern solidification)
- [ ] All P3 child yaks are completed (test coverage and janitorial)
- [ ] `devenv tasks run check:lint` passes
- [ ] `devenv tasks run test` passes
- [ ] `nix flake check` passes on both x86_64-linux and aarch64-darwin

# Notes
This story tracks 17 child yaks organized by priority:
- P0: Dead code removal (OpenClaw legacy, _mkMicrovmV2, stray yak)
- P1: DRY refactoring (Darwin helper, skills dedupe, test aliases, hardening dedupe, server targets)
- P2: Pattern solidification (options split, v1/v2 consolidation, profiles removal, nixpkgs dedupe, test stubs, service registry)
- P3: Test coverage and janitorial (missing option tests, library tests, email script externalization)
