# Goal
The microvm:test devenv task only validates dev-vm. The openclaw microvm
config (targets/microvms/openclaw.nix) is never eval-tested.

# Acceptance Criteria
- [ ] microvm:test task (or a new test:microvm-configs task) evals openclaw.nix
- [ ] Also eval matrix.nix if it exists
- [ ] Test passes in CI

# Files
- devenv.nix (microvm:test task around line 995-1009)
- targets/microvms/openclaw.nix
- targets/microvms/matrix.nix
