# Problem
There are two separate installer implementations that serve overlapping purposes:
- `targets/installer-iso/installer.nix` — NixOS module for the installer ISO target
- `packages/installer/` — A separate Nix package for the installer

These should be reviewed for overlap and consolidated if they duplicate logic.

# Acceptance Criteria
- [ ] Audit what each implementation does and document the differences
- [ ] If one is redundant, remove it and update any references in `flake.nix`
- [ ] If both are needed for different purposes, add comments to each explaining why both exist
- [ ] `nix flake check` passes after consolidation

# Files
- `targets/installer-iso/installer.nix` (NixOS module)
- `packages/installer/` (package, inspect all files here)
- `flake.nix` (update references if needed)
