Root cause: Linux VM role checks evaluated gomuks through insecure olm-3.2.16, but the perSystem check package set and the VM generator's mirrored x86_64-linux package set did not share the permitted insecure package policy used by system builders. Added the shared permittedInsecurePackages list to library/outputs.nix and the VM generator's linuxPkgs fixture, plus a regression assertion. Verified with nix flake check --no-build --system x86_64-linux, nix build .#checks.x86_64-linux.vm-role-generator --no-link, and devenv tasks run check:lint. The broader test:build-checks task still has an unrelated nix-unit NIX_PATH failure.